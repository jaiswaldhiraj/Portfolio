import React from 'react'

export const Hireme = () => {
  return (
            <a
          href={`/contact`}
          className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-[var(--accent)] text-[var(--accent-light)] text-sm font-medium hover:opacity-95 transition"
        >
          Hire me
        </a>
  )
}
